data:
  split_by: userID
  
model:
  type: MessageClassifier
  input_size: 1024
  hidden_sizes:
  - 48
  - 24
  output_size: 2
  dropout: 0.75

training:
  batch_size: 64
  epochs: 300

validation:
  batch_size: 64

optimizer:
  type: adam
  lr: 5e-4
